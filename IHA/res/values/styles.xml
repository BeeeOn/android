<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="Theme.Sherlock.Light.DarkActionBar">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
        <item name="android:windowBackground">@color/iha_background</item>
    	<item name="android:colorBackground">@color/iha_background</item>
        <!-- Support library compatibility -->
        <item name="actionBarStyle">@style/Widget.MyTheme.ActionBar</item>
        <item name="actionModeCloseButtonStyle">@style/NoCloseButton</item>
        <!-- item name="actionBarTabStyle">@style/ActionBarTabStyle.Iha</item>
        <item name="actionModeBackground">@drawable/cab_background_top_iha</item>
        <item name="actionBarStyle">@style/ActionBar.Solid.Iha</item>
        <item name="actionBarTabTextStyle">@style/MyActionBarTabText</item>
        <item name="actionMenuTextColor">@color/white</item-->
        
        <!-- item name="homeAsUpIndicator">@drawable/ic_drawer</item-->
        

    </style>
    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    </style>
       
    <!-- ActionBar styles -->
    <style name="Widget.MyTheme.ActionBar"  parent="@style/Widget.Sherlock.Light.ActionBar">
		<item name="android:background">@color/log_blue2</item>
        <!-- Support library compatibility -->
        <item name="titleTextStyle">@style/Widget.MyTheme.ActionBar.TitleText</item>
        <item name="background">@color/log_blue2</item>

    </style>

    <!-- ActionBar title text -->
    <style name="Widget.MyTheme.ActionBar.TitleText" parent="@style/TextAppearance.Sherlock.Widget.ActionBar.Title">
        <item name="android:textColor">@color/white</item>
        <!-- The textColor property is backward compatible with the Support Library -->
    </style>

    <!-- ActionBar tabs text -->
    <style name="MyActionBarTabText"  parent="Widget.Sherlock.Light.ActionBar.TabText">
        <item name="android:textColor">@color/white</item>
        <!-- The textColor property is backward compatible with the Support Library -->
    </style>
    
    <style name="NoCloseButton" parent="Widget.Sherlock.ActionButton.CloseMode">
        <item name="android:visibility">gone</item>
    </style>
    
    
    <style name="MyDialog" parent="@android:style/Theme.Dialog">
        <item name="android:windowIsTranslucent">true</item>
	    <item name="android:windowNoTitle">true</item>
	    <item name="android:backgroundDimEnabled">true</item>
	    <item name="android:windowIsFloating">true</item>
	    <item name="android:windowBackground">@color/light_gray</item>
	    <item name="android:background">@color/light_gray</item>
    </style>

</resources>